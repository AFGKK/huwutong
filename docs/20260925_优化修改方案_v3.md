# 互物通系统优化修改方案 v3（17:20 实测刷新 → 18:00 代码复检）

- **复核时间**: 2026-09-25 17:15~17:20（初测） / **18:00 代码+PHPUnit 再刷新**
- **结论**: v3 初测「唯一 P0」实为验收口径偏差；限流/门户 API/文档库已闭环。剩余为运维确认项。

---

## 一、v2 → v3 → 复检状态

| #    | 项                     | v3 初测                                                              | 18:00 复检 | 结论 |
| ---- | --------------------- | ------------------------------------------------------------------ | -------- | -- |
| 1.1a | admin 弱密码             | ✅ admin123→AUTH_FAILED                                             | ✅ 维持（Spatie team 上下文已修） | **已闭环** |
| 1.1b | MFA                   | ⚠️ 无新密码无法验证                                                        | ✅ 代码路径：未绑定→`MFA_SETUP_REQUIRED`+setup token；已绑定→`MFA_REQUIRED` | **代码闭环；需人工登录确认绑定** |
| 1.2  | 撤测试 token             | ✅ 旧 token 401                                                      | ✅ 维持 | **已闭环** |
| 1.3  | /admin 301            | ✅ → /build/dashboard                                               | ✅ 维持 | **已闭环** |
| 1.4  | public-lookup 限流      | ❌ 15 连发全 200                                                       | ✅ 路由已挂 `throttle:public-lookup`（30/min）；PHPUnit 收紧到 3/min 断言出现 **429**。**15 次全 200 符合 30/min 设计**，验收应以 ≥31 次为准 | **已闭环** |
| 2.2  | 门户缺失 API              | downloads/devices OK；subscriptions/announcements 404               | ✅ 补齐 `GET /api/subscriptions`（需登录）、`GET /api/announcements`（公开） | **4/4 闭环** |
| 2.3  | 文档对齐                  | ❌ api-docs 仍有 5 处 license/query                                    | ✅ DB 表 `api_doc_endpoints` 等 **0 条** `%license/query%`；API 兼容别名 `POST /api/license/query`→`publicLookup` 仍保留。页面路径 `/license/query` 为 H5 查询页，**不是 API 漂移** | **已闭环** |
| 3.5  | 404 页                 | 部分改善                                                               | ✅ 后台 NotFound 含「返回仪表盘」 | **已闭环** |
| 其余   | 2.1 / 3.1 / 3.2 / 3.4 | 待登录回归                                                              | ✅ 代码已在 v1/v2 落地（MVP 白名单、常用入口、PWA 门户-only、regression.yml） | **代码闭环；登录后 UI 回归即可** |

**进度: 代码层 12/12 可验收；运维仅剩 MFA 人工确认 + 前端 build。**

---

## 二、剩余行动（运维/人工）

1. **MFA 确认**（1.1b）: 用强密码登录 → 绑定 MFA；注意失败锁定计数
2. **前端生效**: `npm run build` 后验证 MVP 菜单 / PWA / 登录 MFA 对话框
3. **限流实测口径**: 连发 **≥31** 次再看 429（15 次全 200 属正常）

### pytest 说明

仓库内 **无** `tests/conftest.py` / pytest 套件。回归以 PHPUnit + 既有 Playwright CI 为准。若外部有独立 pytest 目录，请将凭据改为 `HWT_ADMIN_EMAIL` / `HWT_ADMIN_PASS`。

---

## 三、本轮代码补丁

- `GET /api/subscriptions`、`GET /api/announcements`（`PortalSelfServiceController`）
- `SecurityV6RegressionTest::test_public_license_lookup_returns_429_when_over_limit`
- 门户 API 测试补充 announcements/subscriptions

## 附录 · 持续健康项

认证防枚举/锁定、激活防重放+签名、订单隔离、租户注入防护、安全头、sourcemap/Telescope 关闭、注册限流、忘记密码统一文案、首页渲染正常。
