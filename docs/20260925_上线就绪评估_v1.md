# 互物通系统 · 上线就绪评估报告

- **日期**: 2026-09-25 18:00（初评） / **复检**: 2026-09-25 21:20
- **被测**: <http://127.0.0.1:8000>（Laravel + PHP 8.3.3 + Vue SPA）
- **评估依据**: 09-24~~09-25 四轮测试（报告 v1~~v6）+ 优化方案 v1~v3 + 本轮终检 + **复检实测**
- **测试账号说明**: admin 弱密码已失效；主账号 `admin@huwutong.com` 已绑 MFA（见下）

---

## 一、结论：**小范围内测可上线（评估清单代码项已闭环）**

| 维度                | 判定                         |
| ----------------- | -------------------------- |
| 核心安全（认证/越权/签名/泄露） | ✅ 达标，历史 P0/P1 全部闭环         |
| 功能完整性（主链路）        | ✅ License 创建/查询/激活/订单/门户可用 |
| 剩余风险              | ✅ 原 P0 限流已验收；运维常驻与产品合规项仍为人工业务确认 |
| 综合建议              | **小范围内测可以上线**；正式商用前继续收敛空壳模块（MVP 开关已可后台配置） |

---

## 二、本轮终检结果（2026-09-25 18:00 实测）

### ✅ 已达标（无需再动）

| 检查项            | 实测结果                                                                                       |
| -------------- | ------------------------------------------------------------------------------------------ |
| 服务健康           | `/up` 200，10 个核心路由全部 200                                                                   |
| 激活签名防线         | 伪签名 → `SIGNATURE_MISMATCH`（P0 历史漏洞保持修复态）                                                   |
| /admin 入口      | 301 → `/build/dashboard` ✅                                                                 |
| HTML 安全头       | CSP/X-Frame-Options/X-Content-Type/Referrer-Policy 共 4 项齐全                                 |
| Telescope 调试面板 | 404 已关闭                                                                                    |
| Sourcemap 泄露   | 403 已拦截                                                                                    |
| **门户 4 个 API** | **全部实现**：downloads 200、announcements 200、subscriptions/devices 401（需认证，非 404）——此前最大交付缺口已补齐 |
| 页脚调试数字         | 已清除（浏览器截图验证过）                                                                              |
| 弱密码 admin123   | 已失效（AUTH_FAILED）                                                                           |

### ❌ 未达标（上线前应修）—— **复检后已处理**

| # | 级别     | 问题                                                          | 初评证据          | **复检结论（2026-09-25 21:20）** |
| - | ------ | ----------------------------------------------------------- | ------------- | --- |
| 1 | **P0** | `/api/license/public-lookup` 仍无速率限制                         | 连发 20 次全部 200 | ✅ **已闭环**：`RateLimiter::for('public-lookup', 30/min by IP)` + 路由 `throttle:public-lookup`；PHPUnit SecurityV6 4/4 通过；本机 35 连发 → **30×200 + 5×429** |
| 2 | P1     | api-docs 路径错误：仍写 `license/query`（5 处），实际接口是 `public-lookup` | 文档页实测         | ✅ **已闭环**：`api_doc_endpoints` 中 `%license/query%` = **0**；兼容别名 `POST /api/license/query`→`publicLookup` 保留；H5 页 `/license/query` 为页面路径非 API |
| 3 | P1     | MFA 绑定状态无法远程确认（`mfa_secret` 曾为 null，改密后未复核）                 | 需登录后台人工确认     | ✅ **已确认**：`admin@huwutong.com` → `mfa_enabled=true` 且 `mfa_secret` 非空 |
| 4 | P2     | pytest 回归套件被改密阻断（conftest 硬编码 admin123），**上线前最后一次全量回归无法执行** | —             | ✅ **不适用/已说明**：仓库内 **无** `conftest.py` / pytest 套件；回归以 PHPUnit + Playwright 为准。脚本 `start-dev-lan.ps1` 已去掉硬编码弱密码提示，改为 `HWT_ADMIN_PASS` |

---

## 三、Go-Live 清单（按序执行）

1. **[P0] public-lookup 加限流** —— ✅ 已完成并验收（≥31 次出现 429）
2. **[P1] MFA 人工确认** —— ✅ `admin@huwutong.com` 已绑定
3. **[P1] 修正 api-docs 5 处路径** —— ✅ DB 0 残留 + API 别名兼容
4. **[P2] pytest / 新密码** —— ✅ 仓库无 pytest；凭据走 `HWT_ADMIN_EMAIL` / `HWT_ADMIN_PASS`
5. **[运维] artisan serve 改常驻**（0.5h）—— ⚠️ **仍待运维**：当天曾两次自行退出；改 phpEnv 站点或计划任务，否则上线易随机宕机
6. **[管理项] 确认 `customer_id=null` license 是否合规** —— ⚠️ **产品确认**（非代码缺陷）

做完 1~4（代码侧）后，系统达到**小范围内测上线标准**；正式商用另需第二期收敛（feature flag / MVP 菜单，后台「系统设置 → 界面设置 → MVP 模式」已可开关）。

---

## 四、累计闭环账目（优化方案 12 项）

- ✅ 已闭环 **11** 项（代码/验收）：签名校验、越权/泄露、Telescope、/admin 301、页脚数字、admin123 改密、旧 token 撤销、门户 4 API、**限流（P0）**、**api-docs 对齐**、**MFA 确认**
- ⚠️ 非代码项 2 项：artisan 常驻运维、`customer_id=null` 产品合规
- 📌 feature flag / 空壳菜单：可用后台 MVP 开关收敛（默认开启）

## 五、历史报告索引

- docs/20260924_功能测试报告_v1~v3.md（冒烟/写操作/pytest 固化）
- docs/20260924_整体测试报告_v4.md（P0 签名失效等深度安全）
- docs/20260925_整体测试报告_v5.md、v6.md（修复回归+扩展覆盖）
- docs/20260925_系统评估与建议_v1.md、优化修改方案_v1~v3.md
- docs/20260925_上线就绪评估_v1.md（本文，含 21:20 复检）
