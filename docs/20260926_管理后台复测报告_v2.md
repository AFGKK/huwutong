# 互物通管理后台复测报告 v2

- **日期**: 2026-09-26 10:33~11:05
- **被测**: <http://127.0.0.1:8000/build/dashboard> + 管理员 API 全量
- **性质**: 针对 09-25 报告 v1（docs/20260925_管理后台测试报告_v1.md）P0/P1/P2 的修复回归 + 全量功能复测
- **方法**: 未授权/越权探测复跑（143 条采样）、admin 登录功能抽检、pytest 47 用例、浏览器 UI 走查、源码核对

---

## 一、结论

**v1 全部 P0/P1/P2 已修复并验证通过，管理后台本轮未发现新的安全问题。**  
此前疑点「/build/subscriptions 404」已排除（菜单实际指向 /build/billing，渲染正常）。

| v1 问题                                         | 修复验证                                                                            | 结果                                                  |
| --------------------------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------- |
| P0-1 越权（96/143 admin 路由普通用户可达，含客户 API Key 明文） | `User::tokenAbilities()` 按角色发放 abilities（customer/admin/super-admin），替换 `['*']` | ✅ **0/143 LEAK**，customer-api-keys/sessions 等全部 403 |
| P0-2 /api/orders 泄露他人订单                       | 已按用户隔离                                                                          | ✅ 新用户 0 行，admin 9 行                                 |
| P1-1 部分 admin 路由无能力中间件（activity-feed 等）       | 已覆盖                                                                             | ✅ 普通用户 403「Invalid ability provided」                |
| P2-1 /api/refunds 500（tenant_id null）         | 已修复                                                                             | ✅ 普通用户 200/空列表，不再泄露堆栈                               |
| P3-1 双重前缀路由 api/api/admin/deletion/records    | 已清理全部 `api/api/*` 重复注册（含 deletion/shop/changelog 等） | ✅ **已处理** |
| 疑点 /build/subscriptions 404                   | 菜单实际指向 `/build/billing`                                                         | ✅ 排除，页面正常渲染                                         |

## 二、验证明细

### 2.1 越权探测（与 v1 同口径对比）

- `tests/admin_dashboard_probe.py`：未授权 12/12 → 401；普通用户访问 admin/* 全部 403
- `tests/admin_leak_forensic.py`：143 条采样 admin GET 路由，**LEAK 96 → 0**
- 源码核对：`AuthController` 8 处 `createToken` 均改为 `$user->tokenAbilities()`；`User::tokenAbilities()` 按 spatie 角色发放（普通=customer，admin/tenant-admin=+admin，super-admin=+admin+super-admin）
- 数据隔离双向验证：普通用户 orders/licenses 仅见自身（0 行），admin 见全量（9 行）

### 2.2 pytest 回归

```
47 passed in 177s（独占运行）
```

注：与浏览器走查并行运行时曾出现 10 个 ReadTimeout 假失败——`artisan serve` 为单线程 PHP，并发请求会排队超时。**回归必须独占运行**，或生产环境换 php-fpm/octane。

### 2.3 admin 功能抽检（MFA 登录，本地 TOTP 一次通过）

订单 9 行 / License 3 行 / customer-api-keys 200 / refunds 200 / sessions、SSO、system-health、accounting 全部 200。

### 2.4 UI 走查

仪表盘（统计 3/3/0 正确）、License 管理、订单管理、**订阅计费 /build/billing（"订阅计费"标题正常）** 均正常渲染，无掉登录、无 404。

## 三、遗留/建议

| 项 | 状态（本轮处理） |
|----|------------------|
| 1. artisan serve 单线程并发超时 | **部署项**：上线须 php-fpm+nginx 或 Octane（非代码缺陷） |
| 2. P3 `api/api/*` 双前缀路由 | **已清理**：移除 shop/changelog/revenue/sdk-cache/team/prepaid/public-keys/text-to-sql/data-anonymization 等重复注册；`admin/deletion` 此前已删 |
| 3. MFA 登录接口限流 | **已补**：`POST /api/mfa/login` → `throttle:10,1`；`check-required` → `throttle:30,1` |
| 4. 旧 token 失效 | ✅ 复测已确认正常 |
| 5. 历史测试用户清理 | 可选运维项（未自动删库） |

## 六、本轮代码跟进（相对 v2 复测后的遗留建议）

1. 清理全部 `routes/api/*` 内 `Route::prefix('api/...')` 双重前缀别名（前端本就使用 `/api` + `/shop` 等正确路径）。
2. 为 MFA 登录补限流，降低暴力撞码风险。
3. 回归用例扩展：`AdminAccessControlRegressionTest` 增加双前缀与 MFA throttle 断言。

## 四、测试数据

本轮新增探测账号 2 个 + pytest 残留 2 个，**均已停用清理**（id 35414/35416~35420/35423）。

## 五、产物

- `tests/_forensic_out2.txt`（0 LEAK 证据）、`tests/_pytest_out.txt`
- `screenshots/20_admin_billing.png`
