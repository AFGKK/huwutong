# 互物通系统 · 空壳模块收敛清单

- **日期**: 2026-09-25 21:30（初稿） / **处理**: 2026-09-25 21:50
- **方法**: `artisan route:list` 导出后端 4383 条路由（去参 3965 个 URI），与前端 admin SPA 的 340 个有效模块路由做差集，再对差集做同义词反查排除误报
- **重要更正**: 此前评估报告中「大量模块只有壳」的说法**过于悲观**——实测 340 个前端模块中约 243 个有后端路由支撑（同域命中），确认无后端的只有 4 个（进一步同义词复核后，4 个中仅 1 个为真·路由未挂）

---

## 一、结论

**空壳问题远比之前评估的小，真正的收敛问题是「范围失控」**——一个授权管理系统挂了 340 个前端模块（含 istio/redis-ha/chaos-engineering 这类基础设施运维页面），后端也相应膨胀到 4383 条路由。绝大多数模块前后端都有，缺的是打磨和验证。

**信息架构**：默认用后台「系统设置 → 界面设置 → MVP 模式」只展示核心层；关闭 MVP 可看全量进阶/生态菜单。

---

## 二、初判「真空壳」4 个 — 复核与处理

| 模块 | 前端路径 | 初判 | **复核** | **处理** |
| --- | --- | --- | --- | --- |
| License Key 前缀 | `/license-key-prefix` | ❌ 无路由 | ⚠️ Controller 方法已有，**路由未注册** | ✅ 已挂：`GET /licenses/{id}/key-format`、`POST /licenses/batch-key-format`、`POST /admin/license-key/prefix-migrate` |
| 测试用例生成器 | `/ai/test-generator` | ❌ | ✅ 已有 `/ai/generate-all-tests`、`/ai/test-frameworks` | 保留（AI 套件入口） |
| 两阶段提交 | `/two-phase-commit` | ❌ | ✅ 已有 `/license/reserve|commit|…`、`/admin/reservations/*` | ✅ 补入侧边栏「核心业务」；MVP 关闭后可见 |
| AI 主动洞察 | `/ai-proactive` | ❌ | ✅ 已有 `/insights/*` | 保留 |

> 初判误报原因：按 URI 字面量 `two-phase-commit` / `ai-proactive` 反查，未映射到同义路由名。

---

## 三、弱匹配 — 抽查与修复（2026-09-25）

| 模块 | 原疑点 | 复核 | 处理 |
| --- | --- | --- | --- |
| marketplace-push / rollout / security | 与 license-marketplace 混淆 | ✅ 独立 `/marketplace/*` 路由齐全 | 保留 |
| bi-export | 错配嫌疑 | ✅ `/admin/bi/*` 齐全 | 保留 |
| pricing-optimizer | 语义过宽 | ✅ `GET /ai/pricing-suggestions` | 保留 |
| knowledge-base | 缺批量/导出 | ⚠️ CRUD 有，batch/export 未挂路由 | ✅ 已挂 `/kb/batch/*`、`/kb/export/markdown` |
| license-approval | 审批流独立？ | ❌ FE 写成 `/admin/licenses/approvals`，BE 为 `/licenses/approvals`；且 `request({})` 调用方式坏掉 | ✅ 路径对齐 + `utils/request` 兼容 axios 风格 |
| im-integration | 错配 accounting | ✅ IM / teams-notifier 有路由；删除调用 `delete` 而客户端只有 `destroy` | ✅ 增加 `delete` 别名 |
| search-center | 错配嫌疑 | ⚠️ advanced-search：`filters/{page}` 缺路由；`search/{page}` 误用 `whereNumber` | ✅ 已修 |
| app-marketplace / cloud-marketplace / developer-earnings 等 | 未逐页冒烟 | 静态路由存在 | 建议登录后冒烟；非阻塞 |
| 门户 ai-chat / live-chat / qr-confirm / tenant-select | 门户流程 | 未本轮改 | 建议单独冒烟 |

---

## 四、验证为完整的（此前被误判，现已证清白）

区块链 License、二级市场、混沌工程、蜜罐、AI 渗透测试、LLM、RAG、MLOps、Istio、Redis-HA、HSM、CRL、GraphQL、gRPC、Zapier、飞书、SCIM、Meilisearch……**全都有后端路由**。

---

## 五、收敛建议（执行状态）

1. ~~砍掉/关闭 4 个真空壳~~ → **改为补齐真缺口**（见第二节）；无后端字面壳已澄清
2. ~~登录后逐页验证弱匹配~~ → **高优先级 7 项已静态复核并修复关键路径问题**；其余建议冒烟
3. **信息架构分层**（真正的痛点）— **已有运行时开关**：
   - **核心层**（MVP 开）：License / 客户 / 订单计费 / 工单 / 系统与安全等
   - **进阶层**（MVP 关）：IM、电商、BI、集成、应用生态等
   - **基础设施层**：仍建议正式商用时移出管理后台；当前随 MVP 关闭才出现在侧栏
4. **验证优先级**：弱匹配已修项 > 剩余冒烟 > IA 运维剥离

### 本轮代码变更摘要

- `routes/api/admin.php`：License Key 前缀、KB 批量/导出、advanced-search page 约束
- `resources/js/utils/request.js`：兼容 `request({ url, method })`
- `resources/js/api/licenseApproval.js`：路径去掉错误的 `/admin` 前缀
- `resources/js/api/teamsNotifier.js`：`delete` 别名
- `resources/js/layouts/AdminLayout.vue`：补「两阶段提交」菜单项

> 备注：静态路由分析（前后端路由存在≠功能可用）仍成立；修复项建议强制刷新后台后点开对应页冒烟。
