# 微信小程序功能差距说明（D-31 / T-21）

> 更新日期：2026-07-20  
> 自检：`php scripts/verify-miniprogram.php` → 结构项通过；**管理后台 AppSecret 仍空**；生产 API 本机未连通。

---

## 当前能力（已具备）

| 模块 | 能力 |
|------|------|
| 查询 | 输入 Key、扫码、最近查询、分享带参打开即查 |
| 结果 | 状态引导、示例 Key、订阅过期提醒、过期续费引导 |
| 激活 | 登录后激活（openid 指纹） |
| 我的 | 绑手机、我的激活、产品商城、套餐定价、帮助中心、协议、客服 |
| H5 打通 | SSO → `localStorage.auth_token`；支付外跳引导 |
| CI | `miniprogram-ci` 本地预览/上传 + GitHub Actions |
| UI | P0–P2：品牌减负、结果页 CTA、查询/激活 loading、激活成功页、列表骨架屏 |

**定位：轻量「查授权 + 激活」**，不做第二个管理后台。

---

## 检查结论：还缺什么？

### ① 先做配置（比写功能更急）

| 项 | 状态 |
|----|------|
| AppID `wx4cab3d98eaf9106b` | ✅ |
| 管理后台 AppSecret | ❌ 空 |
| request 合法域名 | ❌ `https://88.huwutong.com` |
| 业务域名（web-view） | ❌ `88.huwutong.com` |
| 代码上传密钥（CI） | ❌ 下载到 `miniprogram/ci/private.key` 或 Secret `MP_CI_PRIVATE_KEY` |
| 订阅消息模板 ID | ❌ |

### ② 已完成

| 项 | 状态 |
|----|:----:|
| A1~A5 / B1~B6 | ✅ |
| **B7** miniprogram-ci | ✅ |

### ③ 可选（需商户资质）

| 功能 | 说明 |
|------|------|
| 小程序原生 `wx.requestPayment` | 商户号绑定小程序 AppID |

---

## B7：CI 用法

详见 [`miniprogram/ci/README.md`](../miniprogram/ci/README.md)

```bash
cd miniprogram
npm install
# 放入 ci/private.key 后：
npm run ci:pack
npm run ci:preview   # → ci/preview-qrcode.jpg
npm run ci:upload    # 上传体验版
```

GitHub：Actions → **Miniprogram CI** → Run workflow（需 Secret `MP_CI_PRIVATE_KEY`）

---

## 自检

```bash
php scripts/verify-miniprogram.php
php artisan test --filter=MiniProgram
```
