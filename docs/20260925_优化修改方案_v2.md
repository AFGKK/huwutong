# 互物通系统优化修改方案 v2（状态刷新版）

- **日期**: 2026-09-25 16:50 复核 / **17:00 代码+实测再刷新**
- **变化**: 相比 v1，开发已修复多项；本版刷新全部 12 项状态并给出剩余行动
- **v2 初测问题根因（已修）**:
  1. Spatie teams 下登录未 `setPermissionsTeamId`，`hasRole` 恒 false → 弱密码/MFA 策略被跳过
  2. 租户 `mfa_policy` 仍为 `optional`（已用 `hwt:secure-admin` 升为 `required_for_admin` 并轮换密码、清理 11 个测试 token）

---

## 一、状态总表（v2 → 再实测）

| #   | 项                   | v2 初测 | 再实测（本机 phpEnv） | 结论 |
| --- | ------------------- | ----- | ---------------- | -- |
| 1.1 | admin 弱密码 + MFA     | ❌ admin123 可登录 | ✅ `admin123`→401 AUTH_FAILED；强密码→`MFA_SETUP_REQUIRED`（仅 setup token） | **已闭环（含根因修复）** |
| 1.2 | 撤销测试 token          | 未验证 | ✅ `hwt:secure-admin` 已撤销 11 个 | **已闭环** |
| 1.3 | /admin 入口 404       | ✅ | ✅ 301→/build | **已闭环** |
| 1.4 | public-lookup 限流    | ❌ 10 次全 200 | ✅ 连发出现 429 | **已闭环** |
| 2.1 | feature flag 收敛空壳模块 | 未复测 | ✅ MVP 白名单 + FEATURES_MVP_MODE | **代码已闭环（需前端 build）** |
| 2.2 | 门户 4 个缺失 API        | 未复测 | ✅ downloads + user/devices（另 2 个延后） | **优先项已闭环** |
| 2.3 | API 文档对齐            | 未复测 | ✅ `/api/license/query` 别名 + 文档路径修正 | **已闭环** |
| 3.1 | 后台 IA 重组            | 未复测 | ✅ 置顶「常用入口」8 项 | **轻量闭环** |
| 3.2 | PWA 弹窗场景化           | 未复测 | ✅ 仅门户 + 30 天 TTL | **已闭环** |
| 3.3 | 页脚 111/333/222      | ✅ | ✅ | **已闭环** |
| 3.4 | CI + UI 冒烟          | 未复测 | ✅ `regression.yml` PHPUnit 只读回归 | **已闭环（Playwright 走既有 CI）** |
| 3.5 | 404 页语义化            | 未复测 | ✅ 返回仪表盘 | **已闭环** |

**进度: 12/12 代码层闭环；subscriptions/announcements 仍按方案延后。**

---

## 二、本轮关键代码修复

- `AuthController::isPrivilegedUser`：登录前注入 Spatie team 上下文
- `MfaService::requiresMfa`：同样注入 team 后再判 admin 角色
- 线上执行：`php artisan hwt:secure-admin`（轮换密码 + MFA 策略 + 撤 token）

---

## 三、运维注意

- 新管理员密码已生成（执行 `hwt:secure-admin` 时终端输出），请立即保存并绑定 MFA 后再进后台
- 前端变更需 `npm run build`（或 Vite 热更新）后 MVP 菜单 / PWA / 登录 MFA 对话框才完全生效
- `artisan serve` 不适合长期运行，建议继续用 phpEnv 常驻站点

## 附录 · 持续健康项

认证防枚举/锁定、激活防重放+签名、订单隔离、租户注入防护、安全头、sourcemap/Telescope 关闭、注册限流、忘记密码统一文案、首页渲染正常。
