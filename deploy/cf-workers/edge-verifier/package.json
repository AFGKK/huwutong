{
  "name": "hwt-edge-verifier",
  "version": "1.0.0",
  "private": true,
  "description": "HWT License Edge Verifier — Cloudflare Worker for <10ms license verification at 200+ global edge nodes",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}
