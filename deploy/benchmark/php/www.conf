; D-39 PHP-FPM 压测池配置（目标 5000 QPS）
[www]
pm = dynamic
pm.max_children = 500
pm.start_servers = 50
pm.min_spare_servers = 25
pm.max_spare_servers = 100
pm.max_requests = 5000
request_terminate_timeout = 30

; 暴露状态页供 nginx/监控（仅内网）
pm.status_path = /fpm-status
ping.path = /fpm-ping
