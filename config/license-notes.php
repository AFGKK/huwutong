<?php

// M2-55 License 内部备注配置

return [

    /*
    |--------------------------------------------------------------------------
    | 备注限制
    |--------------------------------------------------------------------------
    */
    'limits' => [
        // 单条备注最大字符数
        'max_content_length' => 5000,
        // 每条备注最多 @ 提及人数
        'max_mentions' => 10,
        // 每个 License 最多备注数
        'max_notes_per_license' => 500,
    ],

    /*
    |--------------------------------------------------------------------------
    | @提及 通知
    |--------------------------------------------------------------------------
    */
    'mention' => [
        // 被 @ 时是否发送站内通知
        'notify_on_mention' => true,
        // 通知模板
        'notification_template' => '您在 License #{license_id} 的备注中被 @提及',
        // @ 搜索限制
        'search_limit' => 20,
    ],

    /*
    |--------------------------------------------------------------------------
    | 展示配置
    |--------------------------------------------------------------------------
    */
    'display' => [
        // 备注列表默认加载条数
        'default_load_count' => 50,
        // 时间线日期格式
        'date_format' => 'Y-m-d H:i',
        // 是否在列表页显示备注数
        'show_count_in_list' => true,
    ],

    /*
    |--------------------------------------------------------------------------
    | 清理
    |--------------------------------------------------------------------------
    */
    'cleanup' => [
        // 软删除保留天数（0 = 不自动清理）
        'soft_delete_retention_days' => 180,
    ],

];
