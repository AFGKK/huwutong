<?php

// M2-104 UTM/渠道归因追踪 配置

return [
    /*
    |--------------------------------------------------------------------------
    | 支持的 UTM 参数
    |--------------------------------------------------------------------------
    */
    'utm_params' => ['utm_source', 'utm_medium', 'utm_campaign', 'utm_term', 'utm_content'],

    /*
    |--------------------------------------------------------------------------
    | Cookie 过期时间（天）
    |--------------------------------------------------------------------------
    */
    'cookie_ttl_days' => env('UTM_COOKIE_TTL', 30),

    /*
    |--------------------------------------------------------------------------
    | 渠道分组映射
    |--------------------------------------------------------------------------
    */
    'channel_groups' => [
        'Organic' => ['source' => ['google', 'bing', 'baidu', 'sogou', '360']],
        'Paid Search' => ['medium' => ['cpc', 'ppc', 'paidsearch']],
        'Social' => ['medium' => ['social', 'facebook', 'twitter', 'linkedin']],
        'Email' => ['medium' => ['email', 'mail']],
        'Referral' => ['medium' => ['referral', 'link']],
        'Direct' => ['source' => ['direct', '(direct)']],
        'Display' => ['medium' => ['display', 'banner']],
        'Affiliate' => ['source' => ['affiliate', 'partner']],
    ],

    /*
    |--------------------------------------------------------------------------
    | 归因模型
    |--------------------------------------------------------------------------
    */
    'attribution_models' => [
        'first_touch' => '首次接触',
        'last_touch' => '最后接触',
        'linear' => '线性归因',
        'time_decay' => '时间衰减',
    ],

    /*
    |--------------------------------------------------------------------------
    | 默认归因模型
    |--------------------------------------------------------------------------
    */
    'default_model' => env('UTM_DEFAULT_MODEL', 'first_touch'),
];
