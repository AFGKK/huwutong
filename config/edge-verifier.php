<?php

/**
 * Edge 授权验证配置 (M3-53)
 *
 * Cloudflare Workers 边缘节点 License 验证
 * - 全球 200+ 节点 <10ms 验证
 * - HMAC-SHA256 签名 Token
 * - KV 缓存加速
 * - 回源降级
 */
return [

    /*
    |--------------------------------------------------------------------------
    | 签名密钥
    |--------------------------------------------------------------------------
    |
    | 用于签发 HMAC-SHA256 Token 的密钥。生产环境应在 .env 中设置：
    | EDGE_VERIFIER_SECRET=your-64-char-random-secret
    |
    */
    'signing_secret' => env('EDGE_VERIFIER_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | 密钥版本
    |--------------------------------------------------------------------------
    |
    | 用于密钥轮换，每次轮换时增加版本号。
    | Cloudflare Worker 端的密钥也需要同步更新。
    |
    */
    'key_version' => env('EDGE_VERIFIER_KEY_VERSION', 1),

    /*
    |--------------------------------------------------------------------------
    | Token 有效期 (秒)
    |--------------------------------------------------------------------------
    |
    | 默认 3600 秒 (1 小时)，超过后客户端需重新获取。
    |
    */
    'token_ttl' => env('EDGE_TOKEN_TTL', 3600),

    /*
    |--------------------------------------------------------------------------
    | 边缘缓存 TTL (秒)
    |--------------------------------------------------------------------------
    |
    | 边缘节点 KV 缓存的过期时间。
    |
    */
    'edge_cache_ttl' => env('EDGE_CACHE_TTL', 300),

    /*
    |--------------------------------------------------------------------------
    | 回源超时 (毫秒)
    |--------------------------------------------------------------------------
    |
    | Cloudflare Worker 回源到原始服务器的超时时间。
    |
    */
    'fallback_timeout' => env('EDGE_FALLBACK_TIMEOUT', 2000),

    /*
    |--------------------------------------------------------------------------
    | 回源端点
    |--------------------------------------------------------------------------
    |
    | Cloudflare Worker 回源时请求的内部 API 端点。
    |
    */
    'origin_endpoint' => env('EDGE_ORIGIN_ENDPOINT', '/api/edge/origin-verify'),

    /*
    |--------------------------------------------------------------------------
    | 每节点限流 (每分钟请求数)
    |--------------------------------------------------------------------------
    |
    */
    'rate_limit_per_minute' => env('EDGE_RATE_LIMIT', 1000),

    /*
    |--------------------------------------------------------------------------
    | Cloudflare 配置
    |--------------------------------------------------------------------------
    |
    | 部署 Cloudflare Worker 所需的配置。
    |
    */
    'cloudflare' => [
        'account_id' => env('CLOUDFLARE_ACCOUNT_ID'),
        'api_token' => env('CLOUDFLARE_API_TOKEN'),
        'worker_name' => 'hwt-edge-verifier',
        'zone_id' => env('CLOUDFLARE_ZONE_ID'),
        'routes' => [
            '*.example.com/api/edge/*',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 降级策略
    |--------------------------------------------------------------------------
    |
    | 当源站不可达时的降级行为。
    | allowed: 临时允许通过
    | denied: 拒绝请求
    |
    */
    'degraded_mode' => env('EDGE_DEGRADED_MODE', 'allowed'),

    /*
    |--------------------------------------------------------------------------
    | 熔断器
    |--------------------------------------------------------------------------
    |
    */
    'circuit_breaker' => [
        'failure_threshold' => env('EDGE_CB_FAILURE_THRESHOLD', 5),
        'cooldown_seconds' => env('EDGE_CB_COOLDOWN', 60),
    ],
];
