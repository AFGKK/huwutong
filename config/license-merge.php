<?php

/**
 * License 继承/合并配置 (M3-22)
 */
return [
    /*
    |--------------------------------------------------------------------------
    | 可迁移的 License 状态
    |--------------------------------------------------------------------------
    */
    'migratable_statuses' => ['active', 'suspended'],

    /*
    |--------------------------------------------------------------------------
    | 可跳过（标记记录）的状态
    |--------------------------------------------------------------------------
    */
    'retirable_statuses' => ['expired', 'revoked', 'refunded'],

    /*
    |--------------------------------------------------------------------------
    | 完全跳过的状态
    |--------------------------------------------------------------------------
    */
    'skippable_statuses' => ['pending', 'blacklisted', 'frozen'],

    /*
    |--------------------------------------------------------------------------
    | 合并后操作
    |--------------------------------------------------------------------------
    */
    'after_merge' => [
        'mark_source' => true,               // 标记源客户 merged_into_customer_id
        'increment_target_merge_count' => true, // 累加目标客户合并计数
        'update_license_metadata' => true,    // License 记录合并审计
        'allow_source_login' => false,        // 合并后源客户是否允许登录
    ],

    /*
    |--------------------------------------------------------------------------
    | 回滚配置
    |--------------------------------------------------------------------------
    */
    'rollback' => [
        'enabled' => true,
        'window_hours' => 72,                // 合并后72小时内可回滚
    ],
];
