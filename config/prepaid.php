<?php

/**
 * 预付余额 + 信用系统配置 (M3-56)
 */
return [
    /*
    |--------------------------------------------------------------------------
    | 默认币种
    |--------------------------------------------------------------------------
    */
    'default_currency' => 'CNY',

    /*
    |--------------------------------------------------------------------------
    | 自动充值配置
    |--------------------------------------------------------------------------
    */
    'auto_recharge' => [
        'enabled' => true,
        'default_threshold' => 100,     // 低于此金额触发自动充值
        'default_amount' => 500,        // 默认自动充值金额
        'max_amount' => 50000,          // 单次自动充值上限
        'check_interval_minutes' => 60, // 检查间隔
        'payment_methods' => ['alipay', 'wechat'],
    ],

    /*
    |--------------------------------------------------------------------------
    | 信用额度配置
    |--------------------------------------------------------------------------
    */
    'credit' => [
        'enabled' => true,
        'default_limit' => 0,
        'max_limit' => 1000000,
        'negative_balance_grace_days' => 7,  // 负余额宽限期（天）
        'auto_increase_enabled' => false,     // 是否根据消费行为自动提升额度
    ],

    /*
    |--------------------------------------------------------------------------
    | 充值配置
    |--------------------------------------------------------------------------
    */
    'recharge' => [
        'min_amount' => 1,
        'max_amount' => 999999.99,
        'supported_payment_methods' => ['alipay', 'wechat', 'balance'],
        'admin_can_adjust' => true,      // 管理员可手动调整余额
    ],

    /*
    |--------------------------------------------------------------------------
    | 消费配置
    |--------------------------------------------------------------------------
    */
    'consumption' => [
        'deduct_order' => ['balance', 'credit'],  // 扣减顺序：先余额后信用
        'allow_partial_balance' => true,           // 余额不足时允许部分使用余额+其他支付
    ],
];
