<?php

/**
 * Text-to-SQL 安全护栏配置 (M2-136)
 *
 * 控制自然语言转 SQL 的访问权限和安全策略。
 */
return [

    /*
    |--------------------------------------------------------------------------
    | 允许查询的表
    |--------------------------------------------------------------------------
    |
    | 空数组表示允许所有表。如果要限制，列出允许的表名：
    | ['licenses', 'subscriptions', 'customers', 'invoices']
    */
    'allowed_tables' => env('TEXT_TO_SQL_ALLOWED_TABLES')
        ? explode(',', env('TEXT_TO_SQL_ALLOWED_TABLES'))
        : [],

    /*
    |--------------------------------------------------------------------------
    | 允许查询的字段
    |--------------------------------------------------------------------------
    |
    | 按表名分组设置允许的字段。空数组表示允许该表的所有字段。
    | 如果表不在这个配置中，则允许所有字段。
    */
    'allowed_columns' => [
        // 'licenses' => ['id', 'license_key', 'type', 'status', 'created_at', 'expires_at', 'product_id', 'customer_id'],
        // 'customers' => ['id', 'name', 'email'],
        // 'subscriptions' => ['id', 'status', 'plan', 'price', 'starts_at', 'ends_at'],
    ],

    /*
    |--------------------------------------------------------------------------
    | 额外敏感字段
    |--------------------------------------------------------------------------
    |
    | 除了内置的敏感字段列表外，可以额外配置需要保护的字段名。
    | 包含这些字段的查询结果中将自动脱敏。
    */
    'sensitive_columns' => [],

    /*
    |--------------------------------------------------------------------------
    | 最大返回行数
    |--------------------------------------------------------------------------
    |
    | 所有查询将自动添加 LIMIT 限制，防止全表扫描。
    */
    'max_rows' => env('TEXT_TO_SQL_MAX_ROWS', 100),

    /*
    |--------------------------------------------------------------------------
    | 查询超时时间（秒）
    |--------------------------------------------------------------------------
    */
    'query_timeout' => env('TEXT_TO_SQL_QUERY_TIMEOUT', 10),
];
