<?php

// M2-71 域名白名单验证 配置

return [
    /*
    |--------------------------------------------------------------------------
    | 基本设置
    |--------------------------------------------------------------------------
    */
    'enabled' => env('DOMAIN_WHITELIST_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | 每 License 最多可添加的域名数
    |--------------------------------------------------------------------------
    */
    'max_domains_per_license' => env('DOMAIN_WHITELIST_MAX_DOMAINS', 20),

    /*
    |--------------------------------------------------------------------------
    | 通配符域名支持
    |--------------------------------------------------------------------------
    */
    'wildcard_supported' => env('DOMAIN_WHITELIST_WILDCARD', true),

    /*
    |--------------------------------------------------------------------------
    | 域名匹配范围
    |--------------------------------------------------------------------------
    | - activation: 仅激活时校验
    | - validation: 仅验证时校验
    | - both: 激活+验证均校验
    */
    'default_scope' => env('DOMAIN_WHITELIST_DEFAULT_SCOPE', 'both'),

    /*
    |--------------------------------------------------------------------------
    | 变更审批设置
    |--------------------------------------------------------------------------
    */
    'approval' => [
        'require_for_add' => env('DOMAIN_WHITELIST_APPROVE_ADD', true),
        'require_for_remove' => env('DOMAIN_WHITELIST_APPROVE_REMOVE', false),
        'auto_approve_admin' => true, // 管理员操作自动审批
    ],

    /*
    |--------------------------------------------------------------------------
    | 验证失败处理
    |--------------------------------------------------------------------------
    | - block: 直接拒绝
    | - warn: 仅记录日志，不阻断
    */
    'on_failure' => env('DOMAIN_WHITELIST_ON_FAILURE', 'block'),

    /*
    |--------------------------------------------------------------------------
    | 日志保留天数
    |--------------------------------------------------------------------------
    */
    'log_retention_days' => env('DOMAIN_WHITELIST_LOG_RETENTION', 90),
];
