<?php

/**
 * 所有权转移配置 (M3-65)
 */
return [
    /*
    |--------------------------------------------------------------------------
    | 支持的转移对象类型
    |--------------------------------------------------------------------------
    */
    'transferable_types' => ['license', 'product'],

    /*
    |--------------------------------------------------------------------------
    | 状态流转
    |--------------------------------------------------------------------------
    */
    'statuses' => [
        'pending_source',   // 等待源客户确认
        'pending_target',   // 等待目标客户确认
        'pending_approval', // 等待管理员审批
        'completed',        // 已完成
        'rejected',         // 已拒绝
        'cancelled',        // 已取消
    ],

    /*
    |--------------------------------------------------------------------------
    | 转移费用
    |--------------------------------------------------------------------------
    */
    'fees' => [
        'enabled' => true,
        'default_amount' => 0,
        'currency' => 'CNY',
        'max_percentage' => 20, // 最高可设置转移费用为License价格的百分比
    ],

    /*
    |--------------------------------------------------------------------------
    | 关联数据迁移
    |--------------------------------------------------------------------------
    */
    'migration' => [
        'license_fields' => ['customer_id'],
        'include_devices' => true,
        'include_subscriptions' => true,
        'include_invoices' => false, // 发票通常不转移
        'include_audit_logs' => false,
    ],
];
