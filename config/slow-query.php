<?php

// M2-118 数据库慢查询监控面板 配置

return [
    /*
    |--------------------------------------------------------------------------
    | 启用慢查询监控
    |--------------------------------------------------------------------------
    */
    'enabled' => env('SLOW_QUERY_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | 慢查询阈值（毫秒）
    |--------------------------------------------------------------------------
    | 超过此时间的 SQL 会被记录为慢查询。
    */
    'slow_threshold_ms' => (int) env('SLOW_QUERY_THRESHOLD_MS', 200),

    /*
    |--------------------------------------------------------------------------
    | 采样率
    |--------------------------------------------------------------------------
    | 1 = 全部记录（高 I/O），100 = 1% 采样
    */
    'sample_rate' => (int) env('SLOW_QUERY_SAMPLE_RATE', 1),

    /*
    |--------------------------------------------------------------------------
    | 数据保留天数
    |--------------------------------------------------------------------------
    */
    'retention_days' => (int) env('SLOW_QUERY_RETENTION_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | 清理批次大小
    |--------------------------------------------------------------------------
    */
    'prune_batch_size' => (int) env('SLOW_QUERY_PRUNE_BATCH', 1000),

    /*
    |--------------------------------------------------------------------------
    | 自动告警
    |--------------------------------------------------------------------------
    | 某时段内慢查询数超过此值时触发告警通知
    */
    'alert' => [
        'enabled' => env('SLOW_QUERY_ALERT_ENABLED', true),
        'threshold_per_5min' => (int) env('SLOW_QUERY_ALERT_THRESHOLD', 10),
        'channels' => explode(',', env('SLOW_QUERY_ALERT_CHANNELS', 'log')),
    ],

    /*
    |--------------------------------------------------------------------------
    | 忽略的 SQL 模式
    |--------------------------------------------------------------------------
    | 匹配这些模式的 SQL 不记录。支持 SQL LIKE 语法（大小写不敏感）。
    */
    'ignore_patterns' => [
        'SELECT 1',
        'SELECT @@',
        'SELECT @@session',
        'SHOW %',
        '%information_schema%',
        '%performance_schema%',
    ],

    /*
    |--------------------------------------------------------------------------
    | EXPLAIN 配置
    |--------------------------------------------------------------------------
    */
    'explain' => [
        'enabled' => env('SLOW_QUERY_EXPLAIN_ENABLED', true),
        'max_explain_per_query' => 1, // 每条慢查询最多执行一次 EXPLAIN
    ],
];
