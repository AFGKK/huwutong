<?php

// M2-59 NPS 客户满意度调查 配置

return [
    /*
    |--------------------------------------------------------------------------
    | NPS 评分区间定义
    |--------------------------------------------------------------------------
    */
    'score_ranges' => [
        'promoter' => [9, 10],
        'passive' => [7, 8],
        'detractor' => [0, 6],
    ],

    /*
    |--------------------------------------------------------------------------
    | 调查触发配置
    |--------------------------------------------------------------------------
    */
    'trigger' => [
        // 新用户注册后几天触发
        'days_after_registration' => env('NPS_DAYS_AFTER_REG', 7),
        // 使用产品后几天触发
        'days_after_first_activation' => env('NPS_DAYS_AFTER_ACTIVATION', 14),
        // 两次调查最小间隔天数
        'min_interval_days' => env('NPS_MIN_INTERVAL_DAYS', 90),
        // 调查超时未响应自动过期天数
        'expiry_days' => env('NPS_EXPIRY_DAYS', 14),
    ],

    /*
    |--------------------------------------------------------------------------
    | 调查问题
    |--------------------------------------------------------------------------
    */
    'questions' => [
        'core' => [
            'score' => '您有多大可能向朋友或同事推荐我们的产品？',
            'feedback' => '您能分享一下评分的主要原因吗？',
        ],
        'optional' => [
            'best_feature' => '您最喜欢产品的哪个功能？',
            'improvement' => '您觉得哪方面最需要改进？',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 贬损者自动跟进
    |--------------------------------------------------------------------------
    */
    'detractor_followup' => [
        'enabled' => true,
        // 贬损者自动创建工单的评分阈值 (<= 此值)
        'auto_ticket_threshold' => 6,
        // 工单优先级
        'ticket_priority' => 'high',
        // 工单类型
        'ticket_category' => 'feedback',
    ],

    /*
    |--------------------------------------------------------------------------
    | 仪表盘配置
    |--------------------------------------------------------------------------
    */
    'dashboard' => [
        // 趋势统计天数
        'trend_days' => 90,
        // 目标 NPS 分数
        'target_score' => 50,
    ],
];
