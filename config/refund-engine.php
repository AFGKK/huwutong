<?php

// 自动退款 + 风控决策引擎配置 (M3-11)

return [

    /*
    |--------------------------------------------------------------------------
    | 风险评分阈值
    |--------------------------------------------------------------------------
    */
    'risk_thresholds' => [
        'low' => 30,       // 0-30: 低风险，自动批准
        'medium' => 60,    // 31-60: 中等风险，可自动批准
        'high' => 80,      // 61-80: 高风险，需人工审核
        'critical' => 100, // 81-100: 极高风险，自动拒绝
    ],

    /*
    |--------------------------------------------------------------------------
    | 自动处理配置
    |--------------------------------------------------------------------------
    */
    'auto_processing' => [
        'enabled' => env('REFUND_AUTO_PROCESSING_ENABLED', true),
        'auto_approve_low_risk' => true,     // 低风险自动批准
        'auto_approve_medium_risk' => false,  // 中风险需审查
        'auto_reject_critical' => true,       // 极高风险自动拒绝
        'max_pending_minutes' => 1440,        // 超过24h未处理的自动升级
    ],

    /*
    |--------------------------------------------------------------------------
    | 退款限制
    |--------------------------------------------------------------------------
    */
    'limits' => [
        'max_refund_percentage' => 100,       // 最大退款比例(%)
        'max_refund_per_month' => 5,          // 客户月最大退款次数
        'min_days_between_refunds' => 1,      // 两次退款的间隔天数
        'min_refund_amount' => 1,             // 最小退款金额
        'max_refund_without_review' => 5000,  // 免审核最大金额
        'window_days_for_fast_approval' => 7, // 购买7天内快速退款
    ],

    /*
    |--------------------------------------------------------------------------
    | 通知配置
    |--------------------------------------------------------------------------
    */
    'notifications' => [
        'on_auto_approve' => true,
        'on_auto_reject' => true,
        'on_require_review' => true,
        'on_review_completed' => true,
        'on_failure' => true,
        'channels' => ['database', 'mail'],
    ],

    /*
    |--------------------------------------------------------------------------
    | 佣金回拨
    |--------------------------------------------------------------------------
    */
    'commission_clawback' => [
        'enabled' => true,
        'clawback_percentage' => 100,         // 回拨比例
        'reverse_if_older_than_days' => 90,   // 超过90天不回拨
    ],
];
