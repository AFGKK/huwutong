<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 全局资源白名单
    |--------------------------------------------------------------------------
    |
    | 这些模型的数据库表不强制按 tenant_id 过滤查询。
    | 它们是全局共享资源，所有租户可见但不可修改（超管/系统可写）。
    |
    */
    'models' => [
        \App\Models\Product::class,
        \App\Models\SandboxProduct::class,
        // Feature flags — 使用 OpenFeature + Flagd，非 Eloquent 模型
        // Pricing plans — 后续模块
    ],

    /*
    |--------------------------------------------------------------------------
    | 白名单表名
    |--------------------------------------------------------------------------
    */
    'tables' => [
        'products',
        'sandbox_products',
    ],

    /*
    |--------------------------------------------------------------------------
    | 写入保护
    |--------------------------------------------------------------------------
    |
    | 白名单资源仅允许以下角色写入。
    |
    */
    'write_roles' => [
        'super-admin',
        'admin',
    ],

    /*
    |--------------------------------------------------------------------------
    | API 路由白名单
    |--------------------------------------------------------------------------
    |
    | 这些 API 端点不强制注入 tenant_id 查询参数。
    |
    */
    'routes' => [
        'api/*' => ['products', 'settings'],
    ],
];
