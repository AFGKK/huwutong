<?php

/**
 * 交互式产品演示配置 (M3-70)
 *
 * 管理无需注册的在线体验行为
 * - 会话时长
 * - 种子数据版本
 * - 嵌入脚本配置
 * - CTA 注册引导
 * - 自动清理策略
 */
return [

    /*
    |--------------------------------------------------------------------------
    | 会话配置
    |--------------------------------------------------------------------------
    */
    'session' => [
        'duration_minutes' => env('DEMO_SESSION_MINUTES', 30),
        'extend_minutes' => env('DEMO_EXTEND_MINUTES', 15),
        'max_extensions' => env('DEMO_MAX_EXTENSIONS', 3),
        'heartbeat_interval' => 15, // 心跳间隔秒数
    ],

    /*
    |--------------------------------------------------------------------------
    | 启用状态
    |--------------------------------------------------------------------------
    */
    'enabled' => env('DEMO_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | 种子数据
    |--------------------------------------------------------------------------
    */
    'seed_data' => [
        'version' => '1.0',
        'total_licenses' => 128,
        'total_customers' => 42,
        'monthly_revenue' => 28500,
    ],

    /*
    |--------------------------------------------------------------------------
    | CTA 注册引导
    |--------------------------------------------------------------------------
    */
    'cta' => [
        'title' => env('DEMO_CTA_TITLE', '免费注册'),
        'description' => env('DEMO_CTA_DESCRIPTION', '创建您的账户，开始管理 License'),
        'button_text' => '立即注册 →',
        'redirect_url' => env('DEMO_CTA_REDIRECT_URL', '/register'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 嵌入脚本
    |--------------------------------------------------------------------------
    */
    'embed' => [
        'default_mode' => 'floating', // floating | inline | modal
        'default_position' => 'bottom-right',
        'button_text' => env('DEMO_EMBED_BUTTON_TEXT', '在线体验'),
        'theme_color' => env('DEMO_EMBED_THEME_COLOR', '#409eff'),
        'demo_url' => env('DEMO_URL', 'https://demo.huwutong.com'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 清理策略
    |--------------------------------------------------------------------------
    */
    'cleanup' => [
        'expire_after_minutes' => 30, // 会话无心跳超过N分钟标记为过期
        'delete_after_hours' => 24,   // 已过期/已完成会话N小时后彻底删除
        'max_age_minutes' => 60,      // 清理命令最大年龄阈值
    ],
];
