<?php

/**
 * 设备地理位置记录与地域分布配置 (M2-26)
 *
 * 在设备激活/验证时记录 IP 归属地，支持地域分布可视化
 * 和黑名单区域拦截。
 */
return [

    /*
    |--------------------------------------------------------------------------
    | IP 地理位置解析
    |--------------------------------------------------------------------------
    */
    'ip_resolver' => [
        // 驱动: local (IP-IPDB/纯真离线库) | api (第三方API)
        'driver' => env('GEO_IP_DRIVER', 'local'),

        // 本地数据库路径（IPIP.net 或 纯真格式）
        'database_path' => env('GEO_IP_DATABASE', storage_path('app/ipip.ipdb')),

        // 第三方 API 配置（当 driver=api 时）
        'api' => [
            'url' => env('GEO_IP_API_URL', 'https://api.ip.sb/geoip/{ip}'),
            'key' => env('GEO_IP_API_KEY', ''),
            'timeout' => 5,
        ],

        // 缓存 TTL（秒），减少重复查询
        'cache_ttl' => 86400, // 24小时
    ],

    /*
    |--------------------------------------------------------------------------
    | 黑名单区域
    |--------------------------------------------------------------------------
    */
    'blacklist' => [
        // 国家代码（ISO 3166-1 alpha-2）
        'countries' => env('GEO_BLACKLIST_COUNTRIES', []),

        // 区域（如：Asia/Shanghai 时区）
        'regions' => env('GEO_BLACKLIST_REGIONS', []),
    ],

    /*
    |--------------------------------------------------------------------------
    | 统计配置
    |--------------------------------------------------------------------------
    */
    'stats' => [
        // 统计刷新间隔（分钟）
        'refresh_interval' => 30,
    ],
];
