<?php

// M2-119 告警聚合与疲劳管理 配置

return [
    /*
    |--------------------------------------------------------------------------
    | 告警聚合
    |--------------------------------------------------------------------------
    */
    'aggregation' => [
        'enabled' => env('ALERT_AGGREGATION_ENABLED', true),
        'window_minutes' => 60,               // 聚合窗口
        'max_grouped_events' => 100,           // 每组最大事件数
        'group_by' => ['rule_id', 'severity', 'source_type'], // 默认分组维度
    ],

    /*
    |--------------------------------------------------------------------------
    | 告警疲劳控制
    |--------------------------------------------------------------------------
    */
    'fatigue' => [
        'enabled' => env('ALERT_FATIGUE_ENABLED', true),
        'repetition_threshold' => 5,           // 重复次数阈值（超过后降级）
        'decay_factor' => 0.5,                 // 衰减系数
        'noise_score_threshold' => 50,         // 噪音评分阈值
        'auto_downgrade' => true,              // 自动降级
        'auto_silence_minutes' => 120,         // 自动静默时长
    ],

    /*
    |--------------------------------------------------------------------------
    | 静默规则
    |--------------------------------------------------------------------------
    */
    'silence' => [
        'max_active_silences' => 50,
        'allow_wildcard' => true,
        'default_duration_minutes' => 60,
    ],

    /*
    |--------------------------------------------------------------------------
    | 通知摘要
    |--------------------------------------------------------------------------
    */
    'digest' => [
        'enabled' => env('ALERT_DIGEST_ENABLED', true),
        'interval_minutes' => 30,              // 摘要聚合间隔
        'max_digest_items' => 50,              // 摘要中最大项数
    ],

    /*
    |--------------------------------------------------------------------------
    | 值班轮转
    |--------------------------------------------------------------------------
    */
    'rotation' => [
        'default_schedule' => env('ALERT_ROTATION_SCHEDULE', 'weekly'),
        'reminder_minutes' => 15,              // 未确认告警提醒间隔
    ],
];
