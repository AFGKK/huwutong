<?php

// M2-121 业务指标看板(MRR/ARR) 配置

return [
    /*
    |--------------------------------------------------------------------------
    | MRR/ARR 计算
    |--------------------------------------------------------------------------
    */
    'mrr' => [
        'default_months' => 12,
        'max_months' => 36,
        'target_growth_rate' => env('BUSINESS_MRR_GROWTH_TARGET', 10), // 月目标增长率 %
    ],

    /*
    |--------------------------------------------------------------------------
    | LTV/CAC 参数
    |--------------------------------------------------------------------------
    */
    'ltv_cac' => [
        'discount_rate' => env('BUSINESS_LTV_DISCOUNT_RATE', 0.1),    // 折现率
        'avg_customer_lifetime_months' => env('BUSINESS_AVG_LIFETIME_MONTHS', 24),
        'target_ltv_cac_ratio' => env('BUSINESS_LTV_CAC_TARGET', 3),    // 目标 LTV/CAC 比
    ],

    /*
    |--------------------------------------------------------------------------
    | 续费/流失
    |--------------------------------------------------------------------------
    */
    'retention' => [
        'cohort_months' => 12,        // 同期群分析月数
        'warning_churn_rate' => 5,     // 警告流失率 %
        'critical_churn_rate' => 10,   // 严重流失率 %
    ],

    /*
    |--------------------------------------------------------------------------
    | 转化率
    |--------------------------------------------------------------------------
    */
    'conversion' => [
        'trial_to_paid' => env('BUSINESS_TRIAL_CONVERSION_TARGET', 30),    // 试用转付费目标 %
        'activation_rate_target' => env('BUSINESS_ACTIVATION_TARGET', 70), // 激活率目标 %
    ],

    /*
    |--------------------------------------------------------------------------
    | 报表导出
    |--------------------------------------------------------------------------
    */
    'export' => [
        'max_months' => 60,
        'formats' => ['csv', 'xlsx'],
    ],
];
