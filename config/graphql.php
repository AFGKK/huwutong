<?php

/**
 * GraphQL API 配置 (M3-50)
 */
return [
    /*
    |--------------------------------------------------------------------------
    | 端点
    |--------------------------------------------------------------------------
    */
    'endpoint' => '/api/graphql',

    /*
    |--------------------------------------------------------------------------
    | 查询限制
    |--------------------------------------------------------------------------
    */
    'limits' => [
        'max_depth' => 3,           // 最大嵌套深度
        'max_per_page' => 100,      // 每页最大记录数
        'default_per_page' => 20,   // 默认每页记录数
        'max_batch_queries' => 10,  // 批量查询最大数
    ],

    /*
    |--------------------------------------------------------------------------
    | 注册类型配置
    |--------------------------------------------------------------------------
    */
    'types' => [
        'Tenant', 'User', 'Customer', 'Product',
        'License', 'Device', 'Subscription', 'Invoice',
    ],

    /*
    |--------------------------------------------------------------------------
    | 订阅配置 (WebSocket)
    |--------------------------------------------------------------------------
    */
    'subscription' => [
        'enabled' => false,
        'driver' => 'reverb',       // reverb / pusher
        'channel_prefix' => 'graphql.',
    ],
];
