/**
 * IM「我的消息」左侧一级 Tab。
 * 不含「通知」：消息列表 inbox hub + 顶栏 NotificationBell / 通知中心已覆盖，避免重复入口。
 */
export const USER_CHAT_SIDEBAR_TABS = Object.freeze([
    'messages',
    'friends',
    'favorites',
    'pending',
]);
